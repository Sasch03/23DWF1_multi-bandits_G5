import * as UseBanditSimulation from './useBanditSimulation.js';
import { describe } from 'vitest';
import { test, expect } from 'vitest';

describe('useBanditSimulation.js', () => {

    test('Vitest läuft', () => {
        expect(true).toBe(true);
    });
});