/**
 * Represents an enumeration with the choosable distribution typs.
 * @type {Readonly<{BERNOULLI: string, GAUSSIAN: string}>}
 */
const DistributionTyp = Object.freeze({
    BERNOULLI: "Bernoulli",
    GAUSSIAN: "Gaussian",
});

// TODO: Checken, ob die Dokumentation so in Ordnung ist