/**
 * Represents an enumeration with the choosable distribution typs.
 * @type {Readonly<{BERNOULLI: string, GAUSSIAN: string}>}
 */
export const DistributionTyp = Object.freeze({
    BERNOULLI: "Bernoulli",
    GAUSSIAN: "Gaussian",
});
